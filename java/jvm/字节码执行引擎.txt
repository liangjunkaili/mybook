执行引擎：
    解释执行（通过解释器执行）
    编译执行（通过及时编译器产生本地代码执行）
解释器
编译器

栈：
    栈帧
        局部变量表：存放方法参数、方法内部定义的局部变量，最大容量在编译位class文件时，在Code属性的max_locals中确定
        操作数栈：最大深度在编译位class文件时，在Code属性的max_stacks中确定
        动态连接
        方法返回地址